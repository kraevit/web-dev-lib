// From scractch, initialize the React app
// Render an <App /> component
// Create the <App /> component from scratch
// Have the <App /> component to render 3 or 4 checkboxes with paragpraphs or spans next to it
// like you're making a todo list with some hard-coded items on it

import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

reactDOM.render(<App />, document.getElementById("root"));